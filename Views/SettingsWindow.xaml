<Window x:Class="VProofix.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="V-Proofix Settings" Height="450" Width="600"
        WindowStartupLocation="CenterScreen"
        Background="#F4F4F9" Opacity="0">
    <Window.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.15">
                        <DoubleAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseOut"/>
                        </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Gemini API Key:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
        <PasswordBox x:Name="txtApiKey" Grid.Row="0" Grid.Column="1" Margin="0,5" Padding="5"/>

        <TextBlock Text="Model Name:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
        <TextBox x:Name="txtModelName" Grid.Row="1" Grid.Column="1" Margin="0,5" Padding="5"/>

        <TextBlock Text="Target Language:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
        <ComboBox x:Name="cmbTargetLanguage" Grid.Row="2" Grid.Column="1" Margin="0,5" Padding="5">
            <ComboBoxItem Content="English"/>
            <ComboBoxItem Content="Vietnamese"/>
        </ComboBox>

        <TextBlock Text="Fix Hotkey:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
        <TextBox x:Name="txtFixHotkey" Grid.Row="3" Grid.Column="1" Margin="0,5" Padding="5"/>

        <TextBlock Text="Preview Hotkey:" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
        <TextBox x:Name="txtPreviewHotkey" Grid.Row="4" Grid.Column="1" Margin="0,5" Padding="5"/>

        <TextBlock Text="Prompt Format:" Grid.Row="5" Grid.Column="0" VerticalAlignment="Top" Margin="0,5"/>
        <TextBox x:Name="txtPromptFormat" Grid.Row="5" Grid.Column="1" Margin="0,5" Padding="5" Height="60" TextWrapping="Wrap" AcceptsReturn="True"/>

        <!-- Checks -->
        <CheckBox x:Name="chkShowPreview" Content="Show Preview window before replacing (Optional)" Grid.Row="6" Grid.Column="1" Margin="0,10,0,5"/>
        <CheckBox x:Name="chkPrivacyMode" Content="Privacy Mode (Do not save history)" Grid.Row="7" Grid.Column="1" Margin="0,5,0,10"/>

        <!-- Action -->
        <StackPanel Grid.Row="9" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Exit App" Width="80" Margin="0,0,10,0" Padding="5" Click="ExitApp_Click" Background="#FF4C4C" Foreground="White" BorderBrush="Transparent"/>
            <Button Content="Cancel" Width="80" Margin="0,0,10,0" Padding="5" Click="Cancel_Click" Background="#E0E0E0"/>
            <Button Content="Save" Width="80" Padding="5" Click="Save_Click" Background="#007ACC" Foreground="White" BorderBrush="Transparent"/>
        </StackPanel>
    </Grid>
</Window>
